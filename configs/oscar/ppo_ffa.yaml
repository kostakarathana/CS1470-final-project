seed: 7
experiment_name: "oscar-ppo-ffa"
env:
  name: "pommerman"
  mode: "ffa"
  num_agents: 4
  max_steps: 256
  board_size: 11
  observability: "full"
  communication: false
  board_value_count: 14
algorithm:
  name: "ppo"
  learner_setup: "single_learner"
  opponent_policy: "simple"
  reward_preset: "shaped"
  ppo:
    rollout_steps: 256
    minibatch_size: 64
    update_epochs: 4
training:
  total_steps: 32768
  eval_interval_steps: 2048
  eval_episodes: 16
  save_interval_steps: 4096
  device: "cuda"
  log_dir: "artifacts/oscar/ppo-ffa/logs"
  checkpoint_dir: "artifacts/oscar/ppo-ffa/checkpoints"

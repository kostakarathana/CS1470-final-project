seed: 7
experiment_name: "madreamer-shared-smoke"
env:
  name: "mock_grid"
  num_agents: 2
  grid_size: 5
  max_steps: 16
  task_type: "cooperative"
algorithm:
  name: "shared"
  imagination_horizon: 5
  latent_dim: 64
  hidden_dim: 128
  encoder_channels: 32
training:
  total_steps: 64
  batch_size: 16
  replay_capacity: 512
  learning_rate: 0.0003
  device: "cpu"
  rollout_steps: 16
  eval_episodes: 2
